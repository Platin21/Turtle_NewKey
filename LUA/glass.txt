addBox(x,y,width,height,color?,opacity?)
 addFluid(x,y,width,height,liquid)
 addGradientBox(x,y,width,height,color,opacity,color2,opacity2,gradient)
 addGradientLine(p1,p2)
 addGradientLineList(points...)
 addGradientPolygon(points...)
 addGradientQuad(p1,p2,p3,p4)
 addGradientTriangle(p1,p2,p3)
 addIcon(x,y,id,meta?)                      // For ITEMS TO SHOW 
 addLine(p1,p2,color?,opacity?)
 addLineList(color?,opacity?,points...)
 addLiquid(x,y,width,height,liquid)
 addPoint(coord,color?,opacity?)
 addPolygon(color?,opacity?,points...)
 addQuad(p1,p2,p3,p4,color?,opacity?)
 addText(x,y,text,color?)
 addTriangle(p1,p2,p3,color?,opacity?)
 clear()
 doc(method)
 getAdvancedMethodsData(method?)
 getAllIds()
 getAllObjects()
 getById(id)
 getCaptureControl(uuid)
 getGuid()
 getObjectById(id)
 getSurfaceByName(username)
 getSurfaceByUUID(uuid)
 getUsers()
 listMethods(filterSource?)
 listSources()
 sync()